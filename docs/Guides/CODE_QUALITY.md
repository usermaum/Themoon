# Code Quality Guard (코드 품질 가이드)

프로젝트의 "스파게티 코드"와 "중복 코드"를 방지하기 위한 정적 분석 및 포맷팅 가이드입니다.

## 🛠️ 도구 모음 (Toolchain)

| 영역         | 도구 (Tool)  | 용도                               | 실행 명령어              |
| :----------- | :----------- | :--------------------------------- | :----------------------- |
| **Backend**  | **Pylint**   | 코드 품질, 버그, 중복 코드 검사    | `pylint backend/app`     |
| **Backend**  | **Mypy**     | 정적 타입 검사                     | `mypy backend/app`       |
| **Backend**  | **Black**    | 코드 포맷팅 (스타일 통일)          | `black backend/app`      |
| **Frontend** | **ESLint**   | React/Next.js 베스트 프랙티스 검사 | `npm run lint`           |
| **Frontend** | **Prettier** | 코드 포맷팅 (스타일 통일)          | `npx prettier --write .` |

## 🚀 워크플로우 (Workflow)

AI(Antigravity)와 개발자는 코드를 커밋하기 전에 반드시 아래 절차를 수행해야 합니다.

### 1. Backend 작업 시
```bash
# 1. 포맷팅 (자동 정리)
black backend/app

# 2. 품질 검사 (점수 확인)
pylint backend/app

# 3. 타입 검사
mypy backend/app
```

### 2. Frontend 작업 시
```bash
# 1. 린트 및 포맷팅 확인
cd frontend
npm run lint
```

## ⚠️ 주요 점검 항목
- **Duplicated Code**: 중복된 로직은 반드시 함수나 클래스로 분리합니다.
- **Line Too Long**: 한 줄이 너무 길면 가독성이 떨어지므로 분리합니다.
- **Unused Import**: 사용하지 않는 라이브러리는 제거합니다.
