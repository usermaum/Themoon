# AGENTS.md (한국어 참조용)

> **TheMoon - 커피 로스팅 원가 계산 시스템**
> **스택**: Next.js (App Router) + FastAPI + PostgreSQL (Prod) / SQLite (Dev)
> **버전**: 0.3.0
> **OS**: Windows (WSL2 환경)

---

## 🗣️ 언어 규칙 (필수)

**중요**: 사용자는 **영어 초보자**입니다. 원활한 소통을 위해 모든 상호작용은 반드시 **한국어**로만 진행해야 합니다.

- **출력**: 모든 설명, 상태 업데이트("확인 중..."), 결과는 **한국어**여야 합니다.
- **예외**: 코드, 원본 에러 메시지, 로그 출력은 영어를 유지하되, **반드시** 한국어 설명을 덧붙여야 합니다.
- **톤앤매너**: 전문적이고 도움이 되며 간결하게.

---

## 🛠️ 설정 및 명령어

### 개발 서버

환경을 시작할 때는 **항상** 제공된 스크립트를 사용하세요. 특정 컴포넌트를 디버깅하는 경우가 아니라면 `start_all.sh`나 `npm run dev`/`uvicorn`을 직접 사용하지 마세요.

```bash
# 클린 스타트 (캐시 삭제 및 서버 재시작)
wsl bash dev.sh
```

### 프로세스 관리

서버를 시작하기 전에 포트가 비어있는지 확인하세요.

```bash
# 3500(프론트엔드) 및 8000(백엔드) 포트의 기존 프로세스 종료
lsof -ti :3500,8000 | xargs kill -9
```

### 버전 관리

프로젝트 버전을 올리려면 (세션 종료 시에만 수행):

```bash
# 예시: 패치 업데이트
./venv/bin/python logs/update_version.py --type patch --summary "세션 요약 내용"
```

---

## 🏗️ 대원칙 (워크플로우)

### 1. 7단계 개발 방법론 (필수)

**모든** 프로그래밍 작업 시 다음 순서를 따르세요.

1. **Constitution (원칙)**: 기본 원칙과 제약사항을 수립합니다.
2. **Specify (명세)**: 상세 요구사항(입출력, 데이터 구조)을 정의합니다.
3. **Clarify (명확화)**: 모호한 점을 질문하여 해소합니다. **추측하지 마세요.**
4. **Plan (계획)**: 기술 스택과 아키텍처를 결정합니다. `implementation_plan.md`를 작성합니다.
5. **Tasks (작업 분해)**: 작업을 작고 테스트 가능한 단위로 쪼갭니다. `task.md`를 사용합니다.
6. **Implement (구현)**: 코드와 테스트를 작성합니다.
7. **Analyze (검증)**: 명세와 일치하는지 검증합니다.

### 2. 완료의 정의 (3단계 규칙)

다음 3단계가 모두 끝나지 않으면 작업은 **완료된 것이 아닙니다**.

1. **코드/테스트**: 코드를 작성하고 테스트를 통과합니다.
2. **커밋**: `git commit` (여기서 버전을 올리지 마세요).
3. **문서화 ("5종 세트")**: 커밋 직후 즉시 다음 파일들을 업데이트합니다:
   - `logs/CHANGELOG.md`
   - `Documents/Progress/SESSION_SUMMARY_YYYY-MM-DD.md`
   - `README.md` (버전 동기화)
   - `.claude/CLAUDE.md` (버전 동기화)
   - `.gemini/GEMINI.md` (버전 동기화)

---

## 💻 기술 스택 및 컨벤션

### 프론트엔드

- **프레임워크**: Next.js 14 (App Router)
- **UI 라이브러리**: Shadcn UI (v2) + Tailwind CSS
- **언어**: TypeScript (Strict Mode)
- **컨벤션**:
  - 컴포넌트: `PascalCase` (예: `BeanCard.tsx`)
  - 유틸리티: `camelCase` (예: `formatPrice.ts`)
  - 함수형 컴포넌트 필수.

### 백엔드

- **프레임워크**: FastAPI
- **언어**: Python 3.10+
- **데이터베이스**: SQLite (Dev) / PostgreSQL (Prod)
- **ORM**: SQLAlchemy
- **컨벤션**:
  - 파일: `snake_case` (예: `bean_service.py`)
  - 클래스: `PascalCase` (예: `BeanService`)
  - 함수: `snake_case` (예: `get_bean_by_id`)
  - **타입 힌팅**: 모든 함수에 필수 (예: `def func(a: int) -> str:`)

### 도구

- **Mermaid**: 모든 다이어그램(아키텍처, 플로우, ERD)에 **필수**. 이미지 업로드 금지.

---

## 📝 문서화 규칙

### URL 포맷팅

사용자 인터페이스에서 링크가 클릭 가능하도록 **엄격하게** 다음 형식을 따르세요.

**❌ 나쁨**:
`앱이 http://localhost:3500 에서 실행 중입니다!` (URL 뒤에 공백 없음)
`확인하세요 http://localhost:3500` (앞에 줄바꿈 없음)

**✅ 좋음**:

```text
웹앱이 실행되었습니다:

http://localhost:3500

위 주소로 접속하세요.
```

### 버전 업데이트 기준

- **PATCH** (0.0.x → 0.0.y): 버그 수정 3개 이상 또는 문서 업데이트 5개 이상.
- **MINOR** (0.x.0 → 0.y.0): 새 기능 3-4개 누적 시.
- **MAJOR** (x.0.0 → y.0.0): 호환성이 깨지는 변경 시.

---

## 📋 세션 관리

### 파일

- `Documents/Progress/SESSION_START_CHECKLIST.md`: 시작 시 읽기.
- `Documents/Progress/SESSION_END_CHECKLIST.md`: 종료 시 완료.
- `Documents/Progress/SESSION_SUMMARY_*.md`: 시작 시 최신본 읽기 / 종료 시 새로 작성.

### 프로토콜

1. **시작**: 체크리스트 읽기 -> 최신 요약 읽기 -> 버전 전략 확인.
2. **종료**: 체크리스트 완료 -> 세션 요약 작성 -> 버전 업데이트 (기준 충족 시) -> 모든 문서 동기화.

---

## 📂 핵심 디렉토리

- `backend/`: FastAPI 소스
- `frontend/`: Next.js 소스
- `Documents/Architecture/`: 시스템 설계 및 명세 (진실의 원천)
- `logs/`: 버전 관리 및 변경 로그
- `data/`: 로컬 SQLite 데이터베이스 위치

---

*Generated based on .claude/CLAUDE.md and .gemini/GEMINI.md*
