# Claude Code 상태바 스크립트 사용 가이드

> 컬러풀한 이모지와 실시간 정보를 표시하는 Claude Code 상태바

## 📋 기능

- ✅ 현재 시간 및 날짜 표시
- ✅ Git 브랜치 이름 표시
- ✅ Claude 모델 정보 (Sonnet 4.5)
- ✅ 컨텍스트 윈도우 사용률
- ✅ 세션 ID
- ✅ 프로젝트 버전
- ✅ Claude Pro 사용량 (API 연동)
- ✅ 60초 자동 새로고침 모드
- ✅ 컬러풀한 이모지 디자인

## 🚀 사용법

### 1. 기본 실행 (한 번 출력)

```bash
python3 .claude/statusline.py
```

**출력 예시:**
```
🕐 21:15:59 • 📅 2025-12-18(Thu) • 🌿 main • 🤖 Sonnet 4.5 • 📊 45% • 🆔 abc12345 • 📦 v0.2.1 • 💎 25/100
```

### 2. 연속 모드 (60초 간격)

```bash
python3 .claude/statusline.py --continuous
# 또는
python3 .claude/statusline.py -c
```

**출력 예시:**
```
상태바 모니터링 시작 (새로고침 간격: 60초)
종료하려면 Ctrl+C를 누르세요.

[2025-12-18 21:15:59] 🕐 21:15:59 • 📅 2025-12-18(Thu) • 🌿 main • 🤖 Sonnet 4.5 • 📊 45% • 🆔 abc12345 • 📦 v0.2.1 • 💎 25/100
[2025-12-18 21:16:59] 🕐 21:16:59 • 📅 2025-12-18(Thu) • 🌿 main • 🤖 Sonnet 4.5 • 📊 47% • 🆔 abc12345 • 📦 v0.2.1 • 💎 26/100
...
```

### 3. 커스텀 새로고침 간격

```bash
python3 .claude/statusline.py -c 30  # 30초 간격
python3 .claude/statusline.py -c 120  # 120초(2분) 간격
```

### 4. 상세 모드

```bash
python3 .claude/statusline.py --detail
# 또는
python3 .claude/statusline.py -d
```

**출력 예시:**
```
╔══════════════════════════════════════════════════════════════
║ 🕐 시간: 21:15:59  📅 날짜: 2025-12-18 (Thu)
║ 🌿 브랜치: main
║ 🤖 모델: Sonnet 4.5  📊 컨텍스트: 45%
║ 🆔 세션: abc12345  📦 버전: v0.2.1
║ 💎 사용량: 25/100 (25%)
╚══════════════════════════════════════════════════════════════
```

### 5. 도움말

```bash
python3 .claude/statusline.py --help
# 또는
python3 .claude/statusline.py -h
```

## 🔧 ccusage 명령어와 통합

Claude Code의 기본 상태바 명령어와 함께 사용할 수 있습니다:

```bash
# Claude Code 기본 상태바
npx ccusage@latest statusline

# 커스텀 상태바 (이모지 버전)
python3 .claude/statusline.py
```

## 🎨 이모지 아이콘 설명

| 이모지 | 의미 |
|-------|------|
| 🕐 | 현재 시간 |
| 📅 | 현재 날짜 |
| 🌿 | Git 브랜치 |
| 🤖 | Claude 모델 |
| 📊 | 컨텍스트 사용률 |
| 🆔 | 세션 ID |
| 📦 | 앱 버전 |
| 💎 | Claude Pro 사용량 |

## 🔌 Claude Pro 사용량 API 연동

### 현재 상태

현재는 Anthropic API의 직접적인 사용량 조회 엔드포인트가 없어 placeholder 데이터를 표시합니다.

### API 키 설정 (선택사항)

```bash
export ANTHROPIC_API_KEY="your-api-key-here"
```

### 실제 사용량 추적

로컬에서 사용량을 추적하려면 `claude_usage_api.py` 모듈을 수정하여 다음을 구현할 수 있습니다:

1. **로컬 카운터**: 요청 수를 로컬 파일에 저장
2. **캐싱**: 60초 캐시로 API 호출 최소화 (이미 구현됨)
3. **별도 추적 시스템**: 외부 서비스와 연동

## 📁 파일 구조

```
.claude/
├── statusline.py           # 메인 상태바 스크립트
├── claude_usage_api.py     # Claude 사용량 API 모듈
├── settings.json           # Claude Code 상태바 설정
├── hooks.yaml              # 세션 시작 시 자동 실행 설정
└── README_STATUSLINE.md    # 이 파일
```

## 🛠️ 고급 사용

### Claude Code 상태바 통합 (자동 설정됨)

`.claude/settings.json` 파일:

```json
{
  "statusLine": {
    "command": "python3 .claude/statusline.py",
    "refreshInterval": 60
  }
}
```

이 설정으로 Claude Code가 60초마다 자동으로 상태바를 새로고침합니다.

### Hook으로 상태바 자동 실행 (자동 설정됨)

`.claude/hooks.yaml` 파일:

```yaml
hooks:
  SessionStart:
    - name: statusline
      command: python3 .claude/statusline.py
      description: 세션 시작 시 상태바 표시
      blocking: false
```

이 설정으로 새 세션이 시작될 때마다 자동으로 상태바가 표시됩니다.

### Cron으로 주기적 모니터링

```bash
# crontab -e
*/1 * * * * cd /mnt/d/Ai/WslProject/TheMoon && python3 .claude/statusline.py >> ~/.claude_statusline.log
```

## 🐛 트러블슈팅

### ccusage 명령어가 없을 때

```bash
npm install -g ccusage@latest
```

### Git 브랜치가 표시되지 않을 때

Git 저장소 내에서 실행 중인지 확인:

```bash
git status
```

### Python 버전 확인

```bash
python3 --version  # Python 3.7 이상 필요
```

## 📝 라이선스

이 스크립트는 TheMoon 프로젝트의 일부입니다.

## 🔗 관련 링크

- [Claude Code 공식 문서](https://claude.com/claude-code)
- [ccusage GitHub](https://github.com/anthropics/ccusage)
- [TheMoon 프로젝트](../README.md)

---

**마지막 업데이트**: 2025-12-18
**버전**: 1.0.0
