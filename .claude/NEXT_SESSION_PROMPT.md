# 다음 세션 시작 프롬프트

> **⚠️ 새 세션이 시작되면 이 프롬프트의 내용을 따라야 합니다!**

---

## 🚨 다음 세션 시작 시 Claude Code가 반드시 할 것

### STEP 1: 문서 읽기 (필수)

**순서대로 읽으세요:**

```
1. .claude/RULES_CHECKLIST.md (5분) ← 규칙 상기
2. .claude/SESSION_CONTEXT.md (5분) ← 현재 상태 파악
3. Documents/Progress/SESSION_SUMMARY_2025-10-28_FINAL.md (10분) ← 지난 작업 이해
```

**이 3가지를 읽기 전에는 사용자의 요청을 처리하지 마세요!**

---

### STEP 2: 현재 상태 확인 (필수)

다음 명령어를 실행하고 결과를 확인하세요:

```bash
# 현재 버전 확인
cat logs/VERSION

# 최근 커밋 확인
git log --oneline -3

# 파일 상태 확인
git status
```

**기록:**
- 현재 버전: ___________
- 최근 커밋: ___________
- 상태: 정상 / 오류

---

### STEP 3: 규칙 준수 맹세 (필수)

**다음을 읽고 지키겠다고 다짐하세요:**

```
🎯 I will follow these rules:

1. ✅ URL은 항상 독립된 줄에 작성
   - URL 앞뒤로 공백이나 줄바꿈 필수
   - URL 바로 뒤에 한글/특수문자 절대 금지

2. ✅ 작업 완료 후 바로 커밋
   - 여러 작업을 섞어서 커밋하지 않기
   - 한 기능 완성 = 한 커밋

3. ✅ 한글 커밋 메시지
   - 모든 커밋은 한글
   - 코드/에러는 원본 유지

4. ✅ 기능 완성 후 문서 작성
   - Phase 완성 후 SESSION_SUMMARY 작성
   - 세션 최종 요약 필수

5. ✅ 항상 ./venv/bin 사용
   - 절대 python3 직접 사용 금지
   - ./venv/bin/streamlit, ./venv/bin/python 사용

6. ✅ 모든 응답은 한글로
   - 코드와 에러는 원본 유지
   - 설명은 명확한 한글로

규칙을 어기지 않겠습니다! 💪
```

---

### STEP 4: 기본 정보 확인 (필수)

**현재 상태를 정리하세요:**

```
📌 현재 프로젝트 상태:

버전: v1.5.1
최근 작업: UI 버그 수정 + 사이드바 컴포넌트 분리
앱 상태: localhost:8501에서 정상 실행
마지막 커밋: fce65615 (2025-10-28)

✅ 다중 언어 지원 (v1.3.0)
✅ Claude Desktop UI (v1.4.0)
✅ 활성 페이지 감지 (v1.5.0)
✅ UI 버그 수정 + 컴포넌트 분리 (v1.5.1)

핵심 파일:
- app/components/sidebar.py (새로 생성)
- app/app.py (import 추가)
- 9개 페이지 파일 (render_sidebar() 호출 추가)
- .streamlit/config.toml (showSidebarNavigation = false)
```

---

### STEP 5: 이제 사용자 요청 처리

```
✅ 위 4단계를 모두 완료했다면 이제 사용자의 요청을 처리하세요!

⚠️ 처리할 때 주의사항:

1. URL 규칙 100% 준수
2. 기능 완성 후 바로 커밋
3. 한글 커밋 메시지
4. 테스트 완료 후 문서 작성
5. ./venv/bin 사용
```

---

## 📋 작업 중 체크리스트

### 기능 완성 후 (매번)
```
☐ 기능 테스트 완료
☐ git add로 파일 스테이징
☐ git commit로 한글 메시지로 커밋
☐ 버전 파일 자동 업데이트 확인
☐ git log로 커밋 확인
```

### 세션 종료 전 (필수)
```
☐ SESSION_SUMMARY 문서 작성
☐ 모든 파일 커밋됨 확인 (git status clean)
☐ 버전 파일 최신상태 확인
☐ git log로 전체 커밋 이력 확인
```

---

## 🚨 절대 금지할 것

```
❌ URL을 문장에 붙여 쓰기
   예: "http://localhost:8501에서 실행됩니다!"

❌ 작업을 미루고 한 번에 커밋
   예: 버그 수정 → 테스트 → 문서 작성 → 한 번에 커밋

❌ 영어로 응답하기
   예: "I will implement the feature"

❌ python3 직접 사용
   예: python3 script.py

❌ 문서화 없이 작업 종료
   예: 기능 완성 후 SESSION_SUMMARY 안 쓰고 종료
```

---

## ✅ 성공 기준

세션을 마칠 때 이렇게 확인하세요:

```bash
# 1. 모든 파일이 커밋되었는가?
git status
# → 결과: nothing to commit, working tree clean

# 2. 커밋이 정상적으로 이루어졌는가?
git log --oneline -5
# → 결과: 모두 한글 메시지, feat/fix/docs/chore 포함

# 3. 버전이 정상적으로 업데이트되었는가?
cat logs/VERSION
# → 결과: v1.5.1 이상

# 4. 세션 요약이 작성되었는가?
ls Documents/Progress/SESSION_SUMMARY_2025-10-29*
# → 결과: 최소한 PART1, FINAL 파일 존재
```

---

## 💬 성공 메시지 예시

세션이 성공적으로 완료되면:

```
✅ 모든 규칙을 100% 준수하며 작업 완료!

📊 세션 결과:
- 버전: v1.5.1 → v1.6.0 (업그레이드)
- 커밋: 5개 (모두 한글 메시지)
- 문서: SESSION_SUMMARY_2025-10-29_FINAL.md 작성
- URL: 모두 독립된 줄에 표시
- Git: clean (커밋 대기 사항 없음)

🎉 규칙 준수 완벽! 다음 세션도 이렇게 진행하겠습니다!
```

---

## 🎯 이 프롬프트의 목적

**Claude Code가 매 세션마다:**
1. 규칙을 확실하게 상기하고
2. 지난 진행상황을 파악하고
3. 사용자의 요청에 정확하게 응답하고
4. 규칙을 100% 준수하며 작업하고
5. 전문적으로 진행할 수 있도록 돕기

---

**다음 세션이 시작되면 이 프롬프트를 가장 먼저 읽으세요!** 📖

마지막 업데이트: 2025-10-28 22:00
상태: ✅ 준비 완료
